<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Android 12 ke bawah -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <application
        android:name=".MyStudyBuddyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyStudyBuddy">

        <!-- Launcher -->
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity
            android:name=".presentation.screens.profile.ProfileActivity"/>

        <!-- Auth -->
        <activity
            android:name=".presentation.screens.auth.activity.LoginActivity"/>

        <activity
            android:name=".presentation.screens.auth.activity.RegisterActivity"/>

        <activity
            android:name=".presentation.screens.auth.activity.WelcomeActivity"/>

        <!-- Profile -->
        <activity
            android:name=".presentation.screens.profile.ProfileActivity"/>

        <!-- History -->
        <activity
            android:name=".presentation.screens.history.activity.FileHistoryActivity"/>

        <activity
            android:name=".presentation.screens.history.activity.QuizHistoryActivity"/>

        <activity
            android:name=".presentation.screens.history.activity.SummaryHistoryActivity"/>

    </application>

</manifest>